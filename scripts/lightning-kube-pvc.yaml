kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: lightning-pvc
  namespace: lightning-kube
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard
