apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-btcd:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-btcd:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: lightning-kube-btcd
        chartPath: charts/lightning-kube-btcd
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-btcd"
          image.tag: "{{.DIGEST_HEX}}"
